package org.easymarket.ti.edm.v1;

public enum ITSupplierType {

    //TODO - We should change this enum to a database table(@table). We should allow admins to add supplier type in database.
    MARKET,
    BABY_STORE,
    PHARMACY;
}
